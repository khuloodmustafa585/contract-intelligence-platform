# contract-intelligence-platform

Implemented backend utility layer including:

- File handling (PDF, DOCX reading, upload saving)
- Text preprocessing (cleaning, normalization)
- Smart chunking (clause-based + fallback)
- Centralized constants

Also added test pipeline to validate full preprocessing flow.
